CSC200: Chatbot Implementation Project
======================================

# Authors
- Adian Goldfarb (agoldfa7)
- Paul Ouellette (pouellet)
- Rafaello Sanna (rsanna)
- Yiyao Yu (yyu57)

# Building/Testing
`Cargo` is required to build/run/test the project, to do so:

```
# To compile binary target/release/gcd:
$ cargo build --release

# To run compiled binary directly from cargo:
$ cargo run --release
```

<> vim: set syntax=markdown:
